using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HoldableItem : MonoBehaviour
{
    //in the future there would probably be use for this, but for the moment its very bare
    public Item item;
    
}
